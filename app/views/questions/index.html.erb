<%= render "layouts/navbar" %>

<h1>Preguntas</h1>

<%= link_to "Hacer una Pregunta", new_question_path, class: "btn btn-primary" %>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-8">
            <% @questions.each do |question| %>
                <div class="blog-post">
                    <h2 class="blog-post-title"><%= link_to question.title, question_path(question.id) %></h2>
                    <p class="datos-publicacion"> Preguntó <%= question.user.email %> hace <%= distance_of_time_in_words( Time.now, question.created_at) %> </p>
                </div>
                </br>
            <% end %>
        </div>
    </div>
</div>